import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.nandrikinderhilfe.connect',
  appName: 'Nandri Connect',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
